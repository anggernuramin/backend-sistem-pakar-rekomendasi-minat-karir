-- CreateTable
CREATE TABLE "Karir" (
    "id" TEXT NOT NULL,
    "name" TEXT NOT NULL,
    "description" TEXT NOT NULL,
    "pengembangan_karir" TEXT,

    CONSTRAINT "Karir_pkey" PRIMARY KEY ("id")
);
